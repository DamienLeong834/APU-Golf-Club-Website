# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*~
*.tmp

# Course material that gets dropped into images/ by accident. These are document
# types with no business in a website's asset folder, and this repo is public.
images/*.xlsx
images/*.xls
images/*.csv
images/*.docx
images/*.doc
images/*.pptx
images/*.ppt
images/*.pdf

# Local Netlify folder
.netlify
